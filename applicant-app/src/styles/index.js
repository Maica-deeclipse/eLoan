/**
 * Styles Index
 *
 * Central export for all theme and style utilities
 *
 * Usage:
 *   import { colors, spacing, commonStyles } from '../styles';
 */

export {
  colors,
  spacing,
  fontSizes,
  fontWeights,
  typography,
  shadows,
  borderRadius,
  commonStyles,
  buttonStyles,
  inputStyles,
  badgeStyles,
  modalStyles,
  listStyles,
  alertStyles,
  formStyles,
  navButtonStyles,
  default as theme,
} from './theme';
